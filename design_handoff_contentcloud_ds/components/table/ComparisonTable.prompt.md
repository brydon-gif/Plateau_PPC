**ComparisonTable** — the "Why Choose ContentCloud?" matrix. First value column is ContentCloud, auto-highlighted in red.

```jsx
<ComparisonTable
  columns={['ContentCloud', 'Generic AI Chatbots', 'Traditional Search']}
  rows={[
    { feature: 'Source Citations', values: ['Always, page-level', 'Rarely, inaccurate', 'Links only'] },
    { feature: 'EU Data Residency', values: ['✓ Yes', 'Usually US-based', 'Depends'] },
  ]} />
```

Keep cell copy terse. The brand column always sits first and reads red + semibold.
